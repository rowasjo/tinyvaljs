{
    "name": "tinyvaljs",
    "version": "0.1.0",
    "type": "module",
    "description": "Hash table service for storing and retrieving binary blobs using their SHA-256 hashes as keys.",
    "main": "index.js",
    "scripts": {
        "start": "node ./src/tinyvalapi/index.js",
        "test": "node --test"
    },
    "engines": {
        "node": ">=22.0.0"
    },
    "dependencies": {
        "urlpattern-polyfill": "^10.0.0"
    }
}
